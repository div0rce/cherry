{
  "name": "check-contract-fixture",
  "private": true,
  "scripts": {
    "check": "npm run check:guardrails && npm run check:db",
    "test": "npm run check:run-tests",
    "build": "npm run build",
    "ci:verify": "npm run check && npm run test"
  }
}
