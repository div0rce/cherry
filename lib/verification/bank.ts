// Placeholder for bank-based verification (e.g., Plaid/transaction match).
// Returns null to indicate no automated match yet.
export async function checkBankForMatch(_session: unknown) {
  return null;
}
